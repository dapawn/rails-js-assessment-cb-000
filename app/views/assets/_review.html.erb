<%= render 'assets/index', layout: false %> <!-- added assets/ for when rendered in nested route -->
<%= link_to 'Add Asset', new_applicant_asset_path(@applicant), class: "editAsset", layout: false, data: {toggle: "modal", target: "#formModal" } %>
